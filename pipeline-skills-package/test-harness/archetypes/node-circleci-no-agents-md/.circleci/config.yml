version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:20.10
    steps:
      - checkout
      - run: npm install
      - run: npm test

workflows:
  build-and-test:
    jobs:
      - build
